__pycache__/
*.py[cod]
.DS_Store
dist/
briefs-cache.json
remote-image-cache.json
telegram-delivery.json
webhook-delivery.json
telegram-outbox.json
.editorial-drafts/

# Budget governor scratch (site scan 2026-09-02): the advisory lock and the
# atomic-write temp file beside originals/_ledger.json never ride the
# persist commit.
originals/._ledger.json.lock
originals/._ledger.json.*.tmp

# dev-only: tools/svg_rtl_sweep.mjs needs playwright-core (never in the build path)
node_modules/
package-lock.json
